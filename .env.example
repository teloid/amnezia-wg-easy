WG_HOST=your-public-ip-or-dns
PASSWORD_HASH=$2y$10$replace_with_web_ui_bcrypt_hash
LANG=en
PORT=51821
WG_DEVICE=eth0
WG_PORT=51820
WG_DEFAULT_ADDRESS=10.8.0.x
WG_DEFAULT_DNS=1.1.1.1
WG_ALLOWED_IPS=0.0.0.0/0, ::/0
DICEBEAR_TYPE=bottts
USE_GRAVATAR=true

# Prometheus endpoints:
# - /metrics
# - /metrics/json
ENABLE_PROMETHEUS_METRICS=true

# Optional: bcrypt hash for Basic Auth on /metrics endpoints.
# PROMETHEUS_METRICS_PASSWORD=$2y$10$replace_with_metrics_bcrypt_hash
